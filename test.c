#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

void *mymalloc(unsigned int size); // Returns NULL on error.
int main() {
	printf("salam");
	//int *p = mymalloc(sizeof(int));
//	printf("mymalloc(%d) %p\n", 4, p);
	
return 0;
}
